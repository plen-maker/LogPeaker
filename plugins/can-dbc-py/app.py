# Entry module required by `componentize-py componentize <app>`. The
# `decoder-plugin` world's actual export lives in decoder.py (componentize-py
# resolves an exported interface named `decoder` to a top-level importable
# module of the same name); nothing in this world is exported at the
# world level itself, so this file has nothing to define.
