/target
*.jsonl
# in case a plugin crate is ever built standalone (outside the workspace)
plugins/*/target/
plugins/*/Cargo.lock
__pycache__/
# Blender leaves a .blend1 backup next to every .blend it saves
*.blend1
